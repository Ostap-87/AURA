import { notFound } from "next/navigation";

/**
 * Catch-all для несуществующих путей внутри валидной локали. Без этого
 * файла Next.js вообще не находит совпадающий маршрут для случайного
 * URL (например /ru/что-то-несуществующее) и рендерит родовую 404 из
 * корня приложения, минуя [locale]/not-found.tsx — тот подключается
 * только когда notFound() вызывается ИЗНУТРИ уже совпавшего маршрута
 * в этом сегменте, а не когда маршрут структурно не найден вовсе.
 */
export default function CatchAll() {
  notFound();
}
